-- fmap.hs
-- explain what fmap (*3) (+100) is

-- นำผลลัพธ์ที่ได้จากการเรียกฟังก์ชัน (+100) กับ Argument ที่ได้รับ จากนั้นนำผลลัพธ์ที่ได้ไปเป็น Argument ให้กับฟังก์ชัน (*3)
-- ดังนั้น fmap (*3) (+100) ซึ่งเป็นฟังก์ชันที่ทำการเปลี่ยนค่าของ Argument ที่ได้รับเข้าไปโดยการเพิ่ม 100 และคูณด้วย 3 ตามลำดับ